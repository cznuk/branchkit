{
  "name": "branchkit",
  "version": "0.0.4-beta.1",
  "private": true,
  "description": "Monorepo for branchkit - CLI tool and React components for managing UI component versions",
  "keywords": [
    "ab-testing",
    "cli",
    "components",
    "monorepo",
    "react",
    "switcher",
    "ui",
    "versioning"
  ],
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:all": "turbo run dev",
    "sandbox": "turbo run dev --filter=sandbox",
    "sandbox:full": "turbo run dev --filter=@cznuk/branchkit --filter=sandbox",
    "sandbox:watch": "cd apps/sandbox && npm run watch",
    "marketing": "turbo run dev --filter=marketing",
    "marketing:full": "turbo run dev --filter=@cznuk/branchkit --filter=marketing",
    "marketing:watch": "cd apps/marketing && npm run watch",
    "dev:branchkit": "turbo run dev --filter=@cznuk/branchkit",
    "test": "turbo run test",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check"
  },
  "devDependencies": {
    "oxfmt": "^0.27.0",
    "oxlint": "^1.42.0",
    "turbo": "^2.8.0"
  },
  "packageManager": "npm@10.9.0"
}
